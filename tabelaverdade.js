function teste() {
	let nota = Number(window.prompt('Digite uma nota:'))
	let tipo
	if (média < 4; " Reprovado"; if(média < 7; " Recuperação"; " Aprovado")) {
	    tipo = '<strong>FOI REPROVADO</strong>'
	}  else {
	    tipo = '<strong>ESTÁ EM RECUPERAÇÃO</strong>'
	}  else {
	    tipo = '<strong>FOI APROVADO</strong>'
	}

	let res = document.querySelector('section#result')
	res.innerHTML = `<p>O aluno ${ano} que foi digitado  ${tipo}!</p>`
}